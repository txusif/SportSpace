import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Account",
};

export default function AccountPage() {
  return (
    <h2 className="text-2xl font-semibold text-primary mb-7">Welcome, Txusif</h2>
  );
}
